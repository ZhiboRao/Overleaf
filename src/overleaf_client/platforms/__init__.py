"""Platform-specific integration (macOS, etc).

平台专属集成（macOS 等）。

Named ``platforms`` (plural) rather than ``platform`` to avoid colliding
with Python's stdlib ``platform`` module when py2app flattens subpackage
names into the top-level zip archive.

包名用复数 ``platforms``，是为了避免 py2app 压平子包名时与标准库
``platform`` 模块冲突。
"""
